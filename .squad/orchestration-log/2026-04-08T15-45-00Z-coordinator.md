# Orchestration Log — 2026-04-08T15:45:00Z

**Session topic:** ArgoCD teardown-reapply, sidecar injection recovery, virt-launcher iptables hypothesis
**Spawned by:** anesterov (Coordinator)

## Agents Activated

| Agent | Role | Output |
|-------|------|--------|
| Naomi | Platform Engineer | Attempted full ArgoCD teardown and reapply (two spawns, silent completions) |
| Coordinator | Manual gap-fill | Applied config ArgoCD app, restored cloudinit secret, deleted stuck pod |
| Scribe | Logger / Memory | This entry + D-008 decision inbox + git commit |

## Spawn Context

The squad discovered that all prior manual `oc patch` work was being actively
fought by ArgoCD. The git repository was behind origin/main by 1 commit — the
sidecar changes (profile: default, istio-injection: enabled) were local only and
had never been pushed. ArgoCD was continuously reconciling from the old state on origin.

## Events in Order

1. **Root cause identified:** Local commits with sidecar changes not pushed → ArgoCD
   reverting every manual patch from the upstream (ambient) state.
2. **Push executed:** `git push` sent the sidecar pivot commits to origin/main.
3. **step2-ossm-operators synced:** ArgoCD applied `profile: default` (no ztunnel,
   no ambient). ✅
4. **Sail Operator reconciled:** istiod running, ztunnel absent — sidecar mode active. ✅
5. **step2-ossm-config applied:** Namespace `eshoplite-vm` annotated with
   `istio-injection: enabled`. ✅
6. **Cloudinit secret missing:** `eshoplite-cloudinit` Secret had been deleted during
   the ArgoCD teardown cycle. Restored from `deploy/step1-vm/cloudinit-secret.yaml`. ✅
7. **VM recovered:** `virt-launcher-eshoplite-vm-*` moved from CrashLoopBackOff → Running.
8. **Current pod state:** `virt-launcher-eshoplite-vm-24krm` is 1/2 — sidecar injected,
   Init phase passed (was 0/2), istio-proxy container not yet Ready.

## Open Issue

**Port 15021 connection refused despite bound socket.**

- `/proc/net/tcp` inside the pod shows `0.0.0.0:15021` in LISTEN state.
- Envoy logs confirm: "Readiness succeeded" and "Envoy proxy is ready".
- Kubelet external probe: connection refused.
- `curl 127.0.0.1:15021` from the compute container: connection refused.

**Hypothesis:** KubeVirt masquerade networking setup (run `compute` container startup,
AFTER `istio-validation` init container) overwrites or conflicts with Envoy iptables
rules, or adds DROP/REJECT entries that block access to istio-proxy ports from outside
the Envoy process's own netns view.

**Next:** Naomi to inspect virt-launcher iptables rules during pod startup to confirm
or refute the hypothesis. Decision D-008 written to inbox.

## Status

IN PROGRESS — istio-proxy readiness probe failing; Naomi investigation pending.
